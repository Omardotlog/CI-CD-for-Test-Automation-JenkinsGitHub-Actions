pipeline {
    agent any
    stages {
        stage('Unit Test - Backend') {
            agent {
                docker {
                    image 'jfrench/go-junit-report'
                    reuseNode true               
                    }
            }
            steps {
                dir('bugtracker-backend') {
                   sh 'go test -v ./...'
                }
            }
        }
    }
}